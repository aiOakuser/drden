# Opens http://localhost:8004 in your default system browser (HTTP only).
# Use localhost to avoid browser HSTS/SSL issues that often affect 127.0.0.1.
# Run this after starting the Django server with: python manage.py runserver 8004
Start-Process "http://localhost:8004"
